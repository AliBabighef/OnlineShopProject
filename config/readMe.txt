# for use online store project you must: 
	### use localhost like xampp,namp,... OR real server
	### create database:
		#### ecommerce [db name]
			## usersinfo [table_1] => ID, NameStr, E_Mail, Password, StatusUser 
			## category [table_2] => ID, cate_name
`			## comments [table_3] => ID, comment, date, namID, cateID
			## product [table_4] => ID, productName, productDescription, productPrice, productCity, productImg, statusProduct, cateProduct, NameID, dateProduct
			## replies [table_5] => ID, message, cmtID, productID, userID, DateRep
 
# some things about online store project:
	### responsive all advicess
	### no framwork used in front-end while craet by pure html, css and js without without framwork like boostrap,...etc
	### this project needs lot of things that must change them

# plane :
 	### will i do language files
	### changes some things in front-end like color and font...etc
	### will i put new feature 